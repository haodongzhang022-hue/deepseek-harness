/**
 * Removed: the calendar's viewing state (granularity, time range, selection)
 * is component-local useState in CalendarRoot - no cross-entry or
 * remount-surviving consumer exists yet. This placeholder keeps the path
 * resolvable for stale imports; delete at the next sweep.
 */
export {}
